    /* for loop = repeat a loop a limited amount of times
    //                   for(declare index; condition; step) */




    // We have to use (let) to declare a variable with for loop
    // this <will increase the value of i until is reach the condition in the middle
    // (declare index; condition; step)



    // Exemple of increment
    for(let i = 0; i <=10; i+=1 ){
        console.log(i);

    }

    // Exemple of decrement
    for(let i = 20 ; i>0; i--){
        console.log(i);
    }
    console.log("Happy new year bro") 


    // We can declare the index and step out of the for loop

    /* let i= 10
    for(; i>0;){
        console.log(i);
        i--;
    } */